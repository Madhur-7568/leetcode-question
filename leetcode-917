class Solution {
public:
    string reverseOnlyLetters(string s) {
        int n=s.length();
        int j=n-1;
        int i=0;
        while(i<j){
            if(isalpha(s[i]) && isalpha(s[j])){
                swap(s[i],s[j]);
                i++;
                j--;
            }
            else if(!isalpha(s[i])){
                i++;
            }
            else{
                j--;
            }
        }
        return s;
    }
};
