class Solution {
public:
    bool checkPerfectNumber(int num) {
        int sum=0;
        for(int i=1;i*i<=num;i++){
               if(num%i==0 && i!=num){
                int div=num/i;
                if(i!=1 && i*i!=num){
                sum+=i+div;
                }
                else{
                    sum+=i;
                }
               }
        }
        if(sum==num ){
            return true;
        }
        return false;
    }
};
